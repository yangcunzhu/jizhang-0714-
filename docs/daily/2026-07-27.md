# Day 13 — Stage 2 多账户选择器

> 日期:2026-07-20(周一)
> Stage:**S02-classification-accounts**(Day 13 / 共 7 天)
> 工时预算:6 小时
> 工时实际:~3 小时
> 计划类型:`AI_EXEC` + 用户拍板

---

## 🎯 今日目标

1. ✅ account_picker.dart 从「单一现金占位卡 + SnackBar」改为**多账户选择器**
2. ✅ 列出全部账户(复用 AccountCard,6 类型 emoji + 余额 + 信用卡专项字段)
3. ✅ 选中高亮(主色描边 + 右下角 check)+ 点选切换回调
4. ✅ 顶部「添加账户」跳转 AccountManagementPage
5. ✅ record_sheet 接线(传 selectedAccountId + onAccountSelected)
6. ✅ 20 个 account_picker 测试(替换旧 4 个 SnackBar 占位测试)
7. ⏳ commit + push + 收尾卡

## 🔧 上下文

- ✅ Day 11 (commit 9594668):Schema migration v2 + account_dao CRUD + 18 测试
- ✅ Day 12 (commit 32036fd):账户管理 UI + 主页入口 + 37 测试(130/130)
- ✅ ADR-0018 已接受:AccountCard 为「管理页 / 主页 / 记账 Step 3 共用」— 本日兑现复用

## 🆕 Day 13 关键实现

| 点 | 做法 | WHY |
|---|---|---|
| **复用 AccountCard** | 记账 Step 3 直接嵌 AccountCard,外包一层描边 Container | 兑现 ADR-0018,避免重复渲染 6 类型头像 + 信用卡字段,三处视觉一致 |
| **选中高亮** | 选中账户主色描边 width:2 + 右下角 check_circle(带 Key) | Key 让"选中态"可测(不测 border 颜色,脆弱) |
| **添加账户入口** | 「添加账户」→ Navigator.push(AccountManagementPage) | S02 计划 Day 13 明确"跳管理页";替换 Stage 1 的 SnackBar 占位 |
| **自动默认账户** | record_sheet 仍用 defaultAccountProvider 自动选中首账户 | 打开弹层即可保存,用户仅在需要时切换,保 5 秒 3 步体验 |

## 📂 今日创建/修改文件

```
lib/features/record/
├── presentation/record_sheet.dart                   (改:_StepBody 传 selectedAccountId + onAccountSelected)
└── presentation/widgets/account_picker.dart         (重写:多账户列表 + 选中高亮 + 跳转)

test/features/record/
├── account_picker_test.dart                         (重写:20 用例,替换旧 4 个)
└── record_sheet_test.dart                            (改:bootContainer 预读 accountListProvider + 现金 findsWidgets)
```

## 🐛 遇到问题与解决

| 问题 | 解决 |
|---|---|
| record_sheet 全流程测试 `find.text('现金') is too many` | AccountCard 同时渲染名称"现金"与类型标签"现金"(cash.displayName)→ 断言改 findsWidgets |
| record_sheet_test 可能因 AccountPicker 新 watch accountListProvider(StreamProvider)在 fake_async 下卡死 | bootContainer 预读 `accountListProvider.future` 首事件(与 defaultAccountProvider 同法) |
| 选中态如何可测 | 不测 border 颜色(脆弱),改用带 Key 的 check_circle 图标(`account-selected-<id>`) |

## ✅ 验收清单

- [x] 主页 Step 3 账户下拉,可选 ≥2 个账户(列表 + 点选切换)
- [x] 选中账户高亮 + check 标记
- [x] 「添加账户」跳转账户管理页
- [x] 空账户态文案 + 添加按钮兜底
- [x] 复用 AccountCard(信用卡专项字段 / 不计入净资产标记随之显示)
- [x] flutter analyze 0 issues
- [x] flutter test 全绿 **145/145**(Day 12 = 130 → Day 13 = 145,net +15)
- [x] commit + push + 收尾卡
- [ ] Day 16 真机手验(留待 07-23,含账户切换记账路径)

## 📊 累计进度

| 维度 | Day 11 | Day 12 | Day 13 | 累计 |
|---|---|---|---|---|
| 测试用例 | 93 | 130 | **145** | 145 |
| UI widget | 5 | 6 | 6(account_picker 重写) | 6 |
| ADR | 15 | 16 | 16(无新决策) | 16 |

## 📌 不可重新讨论

- ADR-0015 / 0017 / 0018 已接受(写集 / schema / UI 决策)
- ADR-0012 / 0013 已接受(依赖锁定 / emoji 优先)

## ⚠️ 用户注意

- **Day 13 未做**:信用卡还款倒计时显示(S02 计划标"可选",顺延)
- **主页 Step 3 已联动多账户**:记账时可选任一账户扣款,默认自动选首账户
- **Day 14 范围**(S02 计划):分类 CRUD UI(长按主页分类 → 编辑/重排/删除/新增 + emoji picker)

## 🔄 下一步(Day 14)

按 S02 §📅 时间切片:
- 分类管理 feature(category_management_page + category_edit_sheet + emoji_picker)
- category_grid 长按菜单入口
- category_dao CRUD + 排序

---

**Day 13 完成 = 记账闭环账户维度打通**:多账户可选 + 可切换 + 可新增,Stage 2 账户线收官,Day 14 起转分类线。
