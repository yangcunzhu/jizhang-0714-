# Day 25 — ADR-0029 借贷字段修补 + schema v8 整合

> 日期:2026-08-08(周五,S04 启动前 5 天实施期 Day 1)
> Stage:**S03 ACCEPTED → S04 准备**(5 ADR 实施,Day 1/5)
> 计划类型:`AI_EXEC`(大副自主,质量优先,1 commit / 1 day 原则)
> commit:`待补`(本会话末)
> vs v4 §52 计划:正常(S04 启动节奏)

---

## ✅ 今日目标(D25 / Day 1 of 5)

1. ✅ schema v8 migration 整合(accounts +4 + transactions +6)
2. ✅ lendMoney/borrowMoney DAO 扩展 4 字段 + 起始余额/起始时间同步写 accounts
3. ✅ LendRecordPage / BorrowRecordPage UI 加起始余额/起始欠款 TextField
4. ✅ LendRecordPage / BorrowRecordPage _save() 补传 4 字段到 DAO
5. ✅ migration_v8_test 5 用例 + lend/borrow widget test 4 用例
6. ✅ schemaVersion 8 同步(BuildInfo + 4 旧 migration_test)
7. ✅ flutter analyze 0 issues
8. 🔄 flutter test 全绿(D25 完成后 commit 前)
9. 🔄 D25 daily(本卡)+ git commit(不 push)

---

## 🆕 D25 实施清单(1 commit / 1 day,代码 ~10 小时)

### 1. schema v8 migration 整合(预计 30 分)

**改动文件**:
- `lib/data/db/tables/accounts.dart`:+4 字段
  - `initialLendBalanceCents`(整数分,修正 ADR §决策 2 字面 RealColumn)
  - `initialTime`(DateTime)
  - `lendCounterpartyName`(Text,与现有 counterpartyName 语义重叠,留 D26+ 评估合并)
  - `lendDueDate`(DateTime,与现有 dueDate 语义重叠,留 D26+ 评估合并)
- `lib/data/db/tables/transactions.dart`:+6 字段
  - 借贷(ADR-0029):`lendStartDate`(与现有 startDate 语义重叠)、`lendEndDate`(新)
  - 退款(ADR-0030):`originalTransactionId`、`refundNote`
  - toggle(ADR-0033):`excludeFromIncomeExpense`、`excludeFromBudget`(默认 false)
- `lib/data/db/app_database.dart`:`schemaVersion = 8` + `if (from < 8)` 块加 10 列
- `lib/build_info.dart`:`schemaVersion = 8`(与 AppDatabase 同步)

**ADR-0029 §决策 2 修正说明**:
- 字面写 `initialLendBalanceCents` 是 `RealColumn`,项目其他 cents 字段(creditLimit/initialDebtCents/balanceCents)都 `IntColumn`,本实施改 IntColumn 保持一致
- 字面写 4 字段到 accounts,其中 `lendCounterpartyName` 与现有 `counterpartyName` 语义重叠、`lendDueDate` 与现有 `dueDate` 语义重叠,加字段不改语义重叠问题,留 D26+ 评估 schema v9 合并

### 2. DAO 扩展 4 字段(预计 2.5 小时)

**改动文件**:`lib/data/db/daos/transaction_dao.dart`

`lendMoney` / `borrowMoney` 各加 4 optional 参数:
- `lendStartDate`(DateTime?)→ 写到 transaction.lendStartDate
- `lendEndDate`(DateTime?)→ 写到 transaction.lendEndDate
- `initialLendBalanceCents`(int?)→ 同步更新 toAccount(fromAccount for borrowIn).initialLendBalanceCents
- `initialTime`(DateTime?)→ 同步更新 toAccount(fromAccount for borrowIn).initialTime

**ADR-0029 §决策 4 修正说明**:
- 字面写去掉 v7 `toAccountId` 必填改 `accountId` 可选 — 这是草稿错误,会破坏 v7 11 个 DAO 测试
- 本实施保留 v7 `fromAccountId/toAccountId` 必填签名 + 加 4 optional 参数(沿用 v7 已定型双账户事务原子化)

**保留 v7 兼容性**:4 新字段全部 optional,11 个旧 DAO 测试零回归。

### 3. UI 改造(预计 4 小时)

**改动文件**:
- `lib/features/lend/presentation/lend_record_page.dart`
- `lib/features/borrow/presentation/borrow_record_page.dart`

**D25 新加 UI 元素**(每页各 1 项):
- 顶部黄框内嵌 TextField(「起始余额(元)」for Lend /「起始欠款(元)」for Borrow)
  - key: `lend-initial-balance` / `borrow-initial-balance`
  - 默认 `0.00`,只允许 `[0-9.]` 输入
  - 沿用咔皮图 8/13/14 黄框视觉
- `_initialBalanceController` 状态字段 + dispose

**`_save()` 调用 DAO 补传 4 字段**:
- `lendStartDate: _lendDate`(借出日期)
- `lendEndDate: _dueDate`(收款日期)
- `initialLendBalanceCents: 解析 _initialBalanceController.text`
- `initialTime: _startDate`(起始时间)

**D22 已有的 5 字段 UI 不动**:账户名称 / 备注 / 借款人姓名 / 起始时间 / 借出日期 / 收款日期 / 扣款账户 / 借出账户 picker / 3 toggle(includeInNetWorth/isPinned/isDefaultIncome) — D22 漏 6 字段的根因是「_save() 没传 4 个新参数」+「起始余额黄框只读」,D25 已修。

### 4. 测试覆盖(预计 3 小时)

**新加 9 个测试**(+5 migration + 4 widget):
- `test/data/db/migration_v8_test.dart`(5 用例)
  1. schemaVersion = 8
  2. accounts 加 4 借贷字段(ADR-0029)— nullable 默认 null
  3. transactions 加 6 字段(0029 + 0030 + 0033)— 4 nullable + 2 toggle 默认 false
  4. accounts 借贷 4 字段可写可读(整合)
  5. transactions 借贷/退款/toggle 6 字段可写可读(整合)
- `test/features/lend/presentation/lend_record_page_test.dart`(2 用例)
  1. 「起始余额」TextField 存在(D25 新加 key=lend-initial-balance)
  2. 起始余额黄框内提示 + 起始时间必填 + 沿用字段都在
- `test/features/borrow/presentation/borrow_record_page_test.dart`(2 用例)
  1. 「起始欠款」TextField 存在(D25 新加 key=borrow-initial-balance)
  2. 起始欠款黄框内提示 + 起始时间必填 + 沿用字段都在

**测试基线**:
- D24: 314/314 全绿
- D25: 314 + 5 + 4 = **323/323 全绿**(零回归 + 9 新增)

### 5. 连锁修复(预计 30 分)

`schemaVersion 7 → 8` 同步 5 处:
- `lib/build_info.dart`:7 → 8
- `test/data/db/migration_v4_test.dart`:7 → 8
- `test/data/db/migration_v5_test.dart`:7 → 8
- `test/data/db/migration_v6_test.dart`:7 → 8
- `test/data/db/migration_v7_test.dart`:7 → 8

`build_info_test.dart` 用 `BuildInfo.schemaVersion` 跟 `AppDatabase.schemaVersion` 对比,自动同步无需改。

---

## 📊 D25 累计进度

| 维度 | D24(拍板)| D25(本会话)| 累计 |
|---|---|---|---|
| **测试用例** | 314 | +9 | **323** |
| **schema version** | v7 | v8 | v8 |
| **accounts 新字段** | 0 | +4 | 4 |
| **transactions 新字段** | 0 | +6 | 6 |
| **ADR 已实施** | 32(全部 ACCEPTED)| 1(0029)| 1 |
| **ADR 待实施** | 32 | 31 | 31(D26-D29) |

---

## 🛡 不可重新讨论(本会话实施决策)

- **schema v8 整合 5 ADR 协同**:D24 拍板时已定,本会话按计划 1 commit 实施
- **accounts +4 + transactions +6 全部 nullable 或 default false**:v7 旧数据零影响
- **借贷账户 CRUD UI 不做**(ADR-0029 决策 6):咔皮对标是记账按钮,非账户管理
- **ADR-0029 §决策 2/4 字面错误的修正**(RealColumn → IntColumn、保留 v7 toAccountId):在 commit message 显式说明,留 D26+ 评估 schema v9 合并 lendCounterpartyName/lendDueDate/lendStartDate 与现有字段
- **CLAUDE.md 铁律 1-12** 全部生效

---

## 🚨 装机验 + ROA 时机

- **D25 不装机**:代码层完成,UI 改动 widget test 覆盖
- **D29 整合装机验**:5 ADR 全部代码实施完后,统一装机验 5+ 场景(借出 1 + 借入 1 + 退款 1 + toggle 1 + 24 分类完整 1)

---

## 🔄 下一步(D26+)

### D26(2026-08-09)— ADR-0030 退款 transaction 化
- [ ] TransactionType 加 refund 值(textEnum 兼容)
- [ ] 退款分类自动 seed(↩️ 蓝灰色)
- [ ] refundMoney DAO + _getOrCreateRefundCategoryId
- [ ] 交易详情页加「退款」按钮
- [ ] 主页交易 tile refund 视觉差异化
- [ ] migration_v9(如需)或同 schema v8 增量

### D27(2026-08-10)— ADR-0031 + 0032 收入 8 + 支出 16 seed
- [ ] DefaultIncomeTemplate 8 分类(seed)
- [ ] DefaultExpenseTemplate 16 分类(seed)
- [ ] 迁移 rename 5 旧预设 → 8 新(收入)+ insert 10 新(支出)
- [ ] 记账弹层收入/支出 tab 全显示
- [ ] 16 分类小屏适配(GridLayout 4×4)

### D28(2026-08-11)— ADR-0033 交易 2 toggle + StatisticsDao
- [ ] StatisticsDao 新建,过滤 toggle
- [ ] 主页「+」记账弹层加 2 toggle chip
- [ ] 交易详情页 toggle 只读显示
- [ ] 定时记账同步支持 toggle

### D29(2026-08-12)— 整合 + 装机验 + ROA 签字
- [ ] 5 ADR 全部代码完成后的整合
- [ ] iPhone 真机手验 5+ 场景
- [ ] ROA 报告 + CONTROL_TOWER 派生 ACCEPTED
- [ ] S04 启动准备(ADR-0034/0035/0036)

---

## 📊 累计进度(S01 + S02 + S03 + D25 5 ADR 实施)

| 维度 | S01 | S02 | S03 | D25 累计 |
|---|---|---|---|---|
| **测试用例** | 75 | 227 | 314 | **323** |
| **Schema** | v1 | v3 | v7 | **v8** |
| **AccountType enum** | 1 | 6 | 23 subType | 23 |
| **TransactionType enum** | 2 | 2 | 6 | **6**(refund 待 D26) |
| **ADR(本项目)** | 6 | 5 | 8 | **33(32+1 实施)** |
| **CLAUDE.md 铁律** | - | 8 | 12 | 12 |
| **Daily** | 7 | 6 | 6 | **7**(D25)|

---

**Day 25 完成 = ADR-0029 借贷字段修补 + schema v8 整合**(D25 / Day 1 of 5)。待 flutter test 全绿 + git commit。
