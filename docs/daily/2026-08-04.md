# Day 21 — Stage 3 Day 4:账户模型重做(5 大类 × 23 子类)+ 转账流 + 主页聚合菜单

> 日期:2026-08-04(周一)
> Stage:**S03-credit-card-repayment**(Day 21 / 共 7 天 — D4)
> 计划类型:`AI_EXEC`(用户授权「你来定,质量优先,A+ 标准」)
> commit:(本轮待填 SHA)
> vs v4 §52 计划:超前(账户模型是 v4 §3.1 咔皮对标的核心补齐)

---

## 🎯 今日目标(用户授权「你来定」,质量优先)

按 v4 §3.1 + ADR-0026 §10-12 把「扁平 6 种账户」重做为「5 大类 × 23 子类」真模型,
拆 3 个安全增量,每增量测试全绿再进下一步(守住既有 277 测试零回归)。

---

## 🆕 三个增量(全部完成 + 验证)

### 增量① 数据模型 + schema v6
- `AccountCategory`(5 大类)+ `AccountSubType`(23 子类)枚举:携带 category / displayName /
  品牌 emoji / isLiability / legacyType(向下兼容映射)
- accounts 表加 9 列:subType(主模型) / brandName / isPinned / isDefaultIncomeAccount /
  isDefaultExpenseAccount / initialDebtCents / startDate / dueDate / counterpartyName
- schema v5 → v6 migration + 回填 SQL(旧 type → subType CASE 映射)
- **策略**:additive — 保留 `type` 列向下兼容,新列全部 nullable/有 default,老数据回填
- 测试:migration_v6(5)+ 枚举单测(7)

### 增量② 账户编辑 UI 重做
- `account_form_provider`:subType 主模型 + 富字段 + changeSubType(跨大类清字段);
  `type` 保留派生兜底 → 既有 provider 测试零改动通过
- `account_edit_sheet`:5 大类 → 品牌子类两级选择 + 分类字段表单
  - 信用类:额度 / 起始欠款 / **可用额度(自动算)** / 出账日 / 还款日 / 起始时间
  - 借贷类:本金 / 借款人 / 借出借入日期 / 约定还款日期
  - 资金/充值/理财:初始余额
  - 自定义子类:品牌名输入
  - **4 toggle**:计入净资产 / 特别关注 / 默认收账 / 默认支出
- 测试:弹层 widget 重写(13)

### 增量③ 主页「+」聚合菜单 + 转账流
- `TransactionType` 加 `transfer`(textEnum 免迁移)
- `transferMoney` DAO:资金账户间转账(扣款 -amount / 入款 +amount),3 类场景校验 + 事务原子
- transfer feature:`transfer_form_provider` + `transfer_sheet`(双账户下拉 + 金额 + 备注)
- 主页「+」菜单扩展为 **5 入口**:记一笔 / 转账 / 还款 / 借出 / 借入
  - 借出/借入 → 路由到账户弹层预选借贷子类(AccountEditSheet.initialSubType)
  - isScrollControlled 修 5 项在小屏溢出
- transaction_tile:transfer 显示中性色 + ⇄ 前缀
- 测试:transferMoney DAO(5)+ transfer_sheet(5)+ 主页菜单(1)

---

## 📂 主要改动文件

```
新建:
lib/features/transfer/application/transfer_form_provider.dart
lib/features/transfer/presentation/transfer_sheet.dart
test/data/db/account_subtype_test.dart
test/data/db/migration_v6_test.dart
test/data/db/daos/transfer_money_test.dart
test/features/transfer/presentation/transfer_sheet_test.dart

改:
lib/data/db/tables/accounts.dart(2 枚举 + 9 列)
lib/data/db/tables/categories.dart(TransactionType 加 transfer)
lib/data/db/app_database.dart(schemaVersion 5→6 + migration + seed subType)
lib/data/db/daos/transaction_dao.dart(transferMoney + 转账分类 seed + switch)
lib/build_info.dart(schemaVersion 5→6)
lib/features/account/application/account_form_provider.dart(subType 主模型重做)
lib/features/account/presentation/widgets/account_edit_sheet.dart(5 大类 UI 重做)
lib/features/home/presentation/home_page.dart(「+」菜单 5 入口)
lib/features/home/presentation/widgets/transaction_tile.dart(transfer 显示)
+ 4 个既有测试同步(schemaVersion 断言 / AccountEntry 构造 / 弹层 key)
```

未碰(CLAUDE §11 保护):product-design-v4.html / pubspec.yaml(0 新依赖) /
Info.plist / .github/workflows/* / .gitignore

---

## ✅ 验收

- [x] `flutter analyze` 0 issues
- [x] `flutter test` **303/303 全绿**(原 277 → +26)
- [x] additive 策略守住既有测试零回归
- [ ] ROA 真机手验(等装机)

---

## 🔍 铁律 12 自审(显式声明简化 / 后续补)

**做透**:23 子类品牌、资产/负债判定、信用 6 字段、可用额度自动算、迁移回填、转账 3 类场景边界。

**显式简化(铁律 8 — 不是漏,是分阶段)**:
1. **借贷「扣款账户」资金联动** — 建借贷账户暂只存本金,未自动生成对手资金账户流水
   (ADR-0026 §12 未定死该语义,不猜,留后续)。
2. **账本(ledgers)** — 横切每笔交易查询,风险大,留独立 Stage。
3. **退款按钮**(reverseLastTransaction)/ **账户详情 2-tab**(记账/转账)/ **收入分类 8 子类**
   — ADR-0026 已记,排后续。
4. FAB 文案仍「记一笔 / 还款」(菜单已 5 项),留后续微调避免破测试。

---

## 🔄 下一步

- ROA 真机手验:建各大类账户 + 转账 + 借出/借入 + 可用额度自动算
- 或继续补 §12 借贷资金联动 / 账本 Stage 设计(需产品拍板,铁律 7)
