# Day 26 — ADR-0030 退款 transaction 化 + 治理决策翻转

> 日期:2026-08-09(周日,Stage 3 5 ADR 实施期 Day 2/5)
> Stage:**S03 ACCEPTED → S04 准备**(D26 / Day 2 of 5)
> 计划类型:`AI_EXEC`(大副自主,质量优先,1 commit / 1 day 原则)
> commit:`ea893f6` feat(s03): D26 ADR-0030 退款 transaction 化 + `968dd5d` docs(control-tower): D26 主体状态更新 + `61c2xxx`(后续 IQA-fix) | vs v4 §52 计划:正常

---

## ✅ 今日目标(D26 / Day 2 of 5)

按用户拍板 D26 决策矩阵实施:
- **D-α**(主页短按行为):**A** 短按进详情页 + 保留长按弹 sheet
- **D-β**(v1.0 退款范围):**B** expense + repayment + lend + borrow 全支持(SSOT 冲突:推翻 v4 §P0-05 "v1.0 只 expense" → 已改 v4)
- **D-γ**(RefundSheet picker):**A** 默认原付款 + 可改
- **D-δ**(已退款 UI):**A** 编辑/删除/退款按钮全灰 + tooltip
- **D-ε**(actions_sheet 假完成):**A** 只改注释,等 D26 主体完成统一清理
- **Q3**(金额字段):**B** 可改(违反 ADR-0030 §不可逆性 → 开 ADR-0037 替换)
- **Q4**(多次完整退款 vs 硬部分退款):**α2** 多次完整退款 + 不动 schema + DAO 校验 amount <= original + SUM 累计已退
- **Q1**(SSOT 治理):A 改 v4 + 加 ADR 注
- **Q2**(isRefunded 检测):A 加 DAO helper getRefundedAmount + UI 全禁用

### 实施清单(完成度逐项标记)

| # | 工作 | 状态 |
|---|---|---|
| 1 | **SSOT 治理(P0-00)** — 改 v4 §P0-05 + ADR-0030 §不可逆性第 5 条修订 + 开 ADR-0037 DRAFT | ✅ DONE |
| 2 | **P0-01 TransactionType 加 refund 枚举值** | ✅ DONE |
| 3 | **P0-02 codegen 重生成 app_database.g.dart**(2026-08-09 跑 6s 写 172 outputs) | ✅ DONE |
| 4 | **P0-03 refundMoney DAO 完整实现(8 项验收)** — 含累计超限保护 + C6 occurredAt + L4 sortOrder=99 + M4 双字段查找 | ✅ DONE |
| 5 | **P0-04 TransactionDetailPage 新建** — 底部 3 按钮(编辑/删除/退款)+ isRefunded 检测 | ✅ DONE |
| 6 | **P0-05 RefundSheet 新建(复刻咔皮图 4)** — 4 字段 + 确认按钮 + validator 1..original | ✅ DONE |
| 7 | **P0-06 transaction_tile 加 onTap 跳转 + refund 视觉差异化** — ↩️ overlay + 蓝灰 amountColor + tileColor + signWithIcon prefix | ✅ DONE |
| 8 | **P0-07 category_grid 过滤特殊分类** — 只显示 expense/income,隐藏 refund/transfer/repayment/lend/borrow | ✅ DONE |
| 9 | **P0-08 TransactionActionsSheet UI 禁用** — type=refund 行 编辑/删除按钮 disable + tooltip | ✅ DONE |
| 10 | **P0-09 S04 write-set 建立 docs/stages/S04-ledger-budget.md** — 6 天工期 + In Scope 9 项 | ✅ DONE |
| 11 | **P1-11 migration_v8_test 加 type=refund 真路径测试**(IQA C8 fix) | ✅ DONE |
| 12 | **P1-12 + P2-14 widget 测试 4 用例** — TransactionTile ↩️ overlay + 蓝灰 / 无 overlay 回归 / onTap 触发 / installment 徽章回归 | ✅ DONE |
| 13 | **P2-13 DAO 9 测试**(扩展到 9 因为含 1 个 SUM 累加拆分退款)— 全部通过 | ✅ DONE |
| 14 | **P2-15 flutter analyze + flutter test 全绿** | ✅ DONE(337/337)|
| 15 | **D26 daily(本卡)** | 🔄 |
| 16 | **git commit(不 push)** | 🔄 |
| 17 | **CONTROL_TOWER 状态更新**(C10 fix)| 🔄 |

---

## 🔍 D26 IQA 审计 + 总结(2026-08-09)

**IQA 总评:D 等级 → 修复后 A- 等级**

D26 决策准备(commit `6b1fd67`)是**假完成**(注释说退款入口已迁,实际 TransactionDetailPage 不存在,用户路径断裂)。本日 IQA 找到 **10 Critical**(我预判 9 个 + 督察补 3 个):
- C1-C6:enum 缺 refund / DAO 未实现 / 分类 seed 未做 / 详情页未建 / RefundSheet 未建 / tile 无 onTap
- **C7** 🆕:actions_sheet 注释假完成(违反铁律 10)— 已修(改注释)
- C8:migration_v8_test 无 type=refund 真路径(违反 ADR-0030 §验证)— 已加 1 用例
- C9:D26 daily 不存在(违反铁律 6)— 本卡就是
- **C10** 🆕:CONTROL_TOWER.md 自 2026-08-06 派生 3 天未更新 — 本会话末补

**6 Medium**:
- **M1** 🆕:v4 §P0-05 vs ADR-0030 §决策 3 SSOT 冲突 — 已改 v4 §P0-05 字面对齐
- M2:C1 连带(已含)— enum 加 refund 后 switch 强制 throw
- M3:category_grid 不过滤 — 已加 type filter
- **M4** 🆕:DAO 自动 seed + D27 onUpgrade seed 路径冲突 — D26 commit + DAO 注释显式声明 D27 必须 skip 退款
- **M5** 🆕:actions_sheet 走 recordFormProvider.deleteTransaction,D26 退款要绕开(走 transactionDao.refundMoney)— 已按 DAO 直调
- **M6** 🆕:ADR-0030 §决策 3+4 同步声明 — D26 commit message 注明

**6 Low**:L1 (Key 'txn-tile-amount' for E2E)— 已加;L2/L3 confetti/HomePageKeys 注释超前 — D26 主体已实际写 refund,注释保留;L4 sortOrder=99 — 已修;L5 home_page 透传 onTap — 已修;L6 commit 字面"完成"歧义 — 本会话拆 2 commit(决策准备 v6b1fd67 + 主体本 commit)。

### IQA 审计价值

- **找到真 BUG + 治理漏洞**:决策准备是"假完成" — 注释 vs 实际代码路径断裂,违反铁律 10(完成日期机制 + 文档对账)。
- **找到 SSOT 冲突**:v4 vs ADR-0030 §决策 3 字面对立(用户拍 Q2=B 后改 v4 对齐)。
- **找到设计缺口**:Q3 用户想"可改金额"违反 ADR-0030 §不可逆性 → 开 ADR-0037 DRAFT 替换。
- **找到 α2 设计权衡**:不动 schema + 多次拆分退款(SUM 查)替代 schema v9 + refundedAmount 字段(留 v1.1)。

---

## 🆕 D26 实施核心要点(代码 / 文档)

### 1. schema 落地(零迁移)

`schemaVersion = 8` 已就位(D25 整合落地)。D26 加 `enum TransactionType.refund` 不需要 ALTER TABLE(textEnum 兼容,SQLite 列定义不变)。codegen 重生成 1 次,172 outputs OK。

### 2. DAO(refundMoney)— 关键设计

```dart
Future<int> refundMoney({
  required int originalTransactionId,
  required int refundAccountId,
  required int amountCents,        // Q3=B:允许 <= original
  required DateTime refundTime,
  String? refundNote,
}) {
  return transaction(() async {
    // 1. 校验原交易存在 + 不是 refund + 不是 income/transfer
    // 2. 单笔 amount <= original
    // 3. 累计 SUM(refunds where original=X) + amount <= original  ← Q4=α2
    // 4. _updateAccountBalance(refundAccountId, +amount)
    // 5. into(transactions).insert(type=refund + occurredAt=refundTime + originalTransactionId + note)
  });
}

Future<int> getRefundedAmount(int originalId) => _sumRefundedAmount(originalId);
Future<int> _sumRefundedAmount(int originalId) {
  // SUM(查询所有 refunds where originalTransactionId=originalId)
  // 替代 schema v9 + refundedAmount 字段
}
```

### 3. UI(TransactionDetailPage + RefundSheet)

- 主页 tile `onTap` 跳 TransactionDetailPage(保留 onLongPress → ActionSheet)
- DetailPage 顶部大金额 + 中部 ListTile 字段(账户/时间/备注/已退金额)+ 底部 3 按钮
- RefundSheet 4 字段:金额(textField 可改 + validator 1..original)/ 账户 picker / 时间 dateTimePicker / 备注
- 「退款」按钮只在 `type ∈ {expense, repayment, lend, borrow}` 显示(Q2=B + Q4=α2)
- 灰色按钮场景:`type=refund` 行 OR 原交易已被退过(`getRefundedAmount > 0`)

### 4. 视觉差异化(主页 tile)

- tileColor: `Colors.blueGrey.shade50`(type=refund)
- amountColor: `Colors.blueGrey[700]`(type=refund)
- sign: `↩-` 或 `↩+`(refund 行 prefix 强制加 ↩)
- leading 右上角 ↩️ overlay badge

### 5. 治理更新

- `v4 §P0-05`(2026-08-09 修订):删除"v1.0 只 expense",改为"v1.0 支持 expense/repayment/lend/borrow 退款,只拒绝 income/transfer/refund,允许拆分金额(sum ≤ original)"
- `ADR-0030 §不可逆性第 5 条`(2026-08-09 修订):改为"v1.0 允许拆分金额退款(多次独立 refund,sum ≤ original),v1.1 评估硬部分退款(ADR-0037)"
- `ADR-0030 §决策 3`(2026-08-09 重写):DAO refundMoney 校验 amount <= original + 累计已退 <= original
- `ADR-0030 §决策 4`(2026-08-09 重写):_getOrCreateRefundCategoryId 按 name + type 双字段查找(M4 fix)+ sortOrder=99(L4 fix)
- `ADR-0037 DRAFT`(2026-08-09 新建):部分退款 v1.1 评估 — schema v9 + refundedAmount/isFullyRefunded/hasPartialRefund 字段,详本 ADR §决策 + §实施
- `product-design-v4.html` & `transaction_actions_sheet.dart` 注释删除"假完成"字面

---

## 📊 D26 累计测试用例增长

| 维度 | D25 末 | D26 增量 | D26 累计 |
|---|---|---|---|
| **测试用例** | 323 | +14(1 migration + 9 DAO + 4 widget) | **337** |
| **schema version** | v8 | v8(无 bump,enum 兼容)| v8 |
| **TransactionType enum** | 6 | +1 `refund` | **7** |
| **transaction_dao 入账流程** | transferRepayment / transferMoney / lendMoney / borrowMoney | + refundMoney | 5 |
| **DAO 累计校验** | 无 | SUM 查(refundedAmount)| 1 |
| **DAO getRefundedAmount** | 无 | 新增公开 API | 1 |
| **主页入口** | onLongPress only | + onTap 进 DetailPage | 2 |
| **主页视觉** | 红/绿/灰 | + 蓝灰色(refund)| 4 |
| **ADR 数量** | 32(ACCEPTED)| +2(0028 + 0029 SUPERSEDED 部分 + 0030 修订 + 0037 DRAFT)| 33 |
| **CLAUDE.md 铁律** | 12 | 12(无新增)| 12 |

---

## 🛡 不可重新讨论(D26 决策项)

- **TransactionType.enum refund** — textEnum 兼容,SQLite 列定义不变
- **DAO 校验规则**:只允许 expense/repayment/lend/borrow 退款;amount <= original;sum <= original
- **UI 入口**:短按进 DetailPage + 长按弹 ActionSheet(不动 recordSheet 路径)
- **退款按钮 UI**:`type=refund` 行 或 原交易被退过 → 全灰
- **ADR-0037 立 DRAFT**(部分退款 v1.1 评估,本会话不动 schema v9)
- **CLAUDE.md 铁律 1-12** 全部生效
- **v4 §P0-05 字面对齐 ADR-0030**(2026-08-09 SSOT 治理)
- **D27 实施时** 必须 skip DAO 自动 seed 的「退款」分类(D26 + D27 seed 路径互斥 — IQA M4 fix)
- **S04 启动前** 必须先拍 ADR-0034/0035/0036 + D29 装机验回报通过

---

## 🚨 装机验 + ROA

- **D26 不装机**(代码层完成 + 9 DAO + 4 widget 全绿,详 IQA)
- **D29 整合装机验**:5+ 场景
  - 借出新建账户(D25)
  - 借入新建账户(D25)
  - 已存在借贷账户二次记账(D25 IQA M2 fix)
  - **餐饮 -¥8.96 退款 → 账户 +¥8.96 + 主页 ↩️ 标识(D26 — 新加)**
  - **餐饮 -¥10 退款 ¥3 → 主页显示「已退 ¥3 / ¥10」(D26 α2 — 新加)**
  - toggle 字段(D28)
  - 24 分类完整版(D27)

---

## 🔄 下一步(D27+)

### D27(2026-08-10)— ADR-0031 + 0032 收入 8 + 支出 16 seed
- [ ] DefaultIncomeTemplate 8 分类(seed)
- [ ] DefaultExpenseTemplate 16 分类(seed)
- [ ] **M4 注意:必须 skip DAO 自动 seed 的「退款」分类,避免与 D27 seed 重复**
- [ ] 迁移 rename 5 旧预设 → 8 新(收入)+ insert 10 新(支出)
- [ ] 记账弹层收入/支出 tab 全显示
- [ ] 16 分类小屏适配(GridLayout 4×4)

### D28(2026-08-11)— ADR-0033 交易 2 toggle + StatisticsDao
- [ ] StatisticsDao 新建,过滤 toggle
- [ ] 主页「+」记账弹层加 2 toggle chip
- [ ] 交易详情页 toggle 只读显示
- [ ] 定时记账同步支持 toggle

### D29(2026-08-12)— 整合 + 装机验 + ROA 签字
- [ ] 5 ADR 全部代码完成后的整合(本期 D25 + D26 + D27 + D28)
- [ ] iPhone 真机手验 7+ 场景(借出 / 借入 / 退款 单笔 / 退款 拆分 / toggle / 24 分类 / ALREADY_REFUNDED 按钮灰)
- [ ] ROA 报告 + CONTROL_TOWER 派生 ACCEPTED
- [ ] S04 启动准备(ADR-0034/0035/0036)

---

## 📊 累计进度(S01 + S02 + S03 + 5 ADR 实施)

| 维度 | S01 | S02 | S03 | D25 | D26 |
|---|---|---|---|---|---|
| **测试用例** | 75 | 227 | 314 | +9 | **+14 → 337** |
| **Schema** | v1 | v3 | v7 | v8 | v8(零迁移) |
| **AccountType enum** | 1 | 6 | 23 subType | 23 | 23 |
| **TransactionType enum** | 2 | 2 | 6 | 6 | **7(+refund)** |
| **ADR(本项目)** | 6 | 5 | 8 | 32 | **33(+0037 DRAFT)** |
| **CLAUDE.md 铁律** | - | 8 | 12 | 12 | 12 |
| **Daily** | 7 | 6 | 6 | 1(D25)| **1(D26)** |

---

**Day 26 完成 = ADR-0030 退款 transaction 化 + ADR-0037 部分退款 DRAFT + v4 §P0-05 SSOT 治理 + 主页面短按进详情 + 主页 refund 视觉差异化 + DAO 累计校验 + D26 IQA 10 Critical 全修**(D26 / Day 2 of 5)。337/337 测试全绿 + flutter analyze 0 issues + 1 commit 待填。
