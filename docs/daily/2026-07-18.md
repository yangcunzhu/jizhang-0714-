# Day 4 — Drift Schema 设计 + 初始化

> 日期:2026-07-17(周五)
> Stage:**S01-manual-record**(Day 4 / 共 7 天 · **Stage 1 启动**)
> 工时预算:6 小时
> 工时实际:___
> 计划类型:`AI_EXEC` + 用户拍板

---

## 🎯 今日目标

1. ✅ Stage 0 ROA 签字 + Stage 1 启动
2. ⏳ Drift schema 设计(3 张表:Transaction / Category / Account)
3. ⏳ pubspec.yaml 添加依赖(riverpod, drift, drift_dev, intl, vibration, build_runner)
4. ⏳ Drift 初始化代码(`lib/data/db/app_database.dart`)
5. ⏳ DAO 接口设计(TransactionDao / CategoryDao / AccountDao)
6. ⏳ 数据库 schema 单元测试(开 SQLite in-memory 测试)
7. ⏳ 准备 Day 5 Riverpod 状态管理骨架

## 🔧 上下文

- ✅ Stage 0(2026-07-16 完成):iPhone 真机看到 "Hello 审计官"
- ✅ ADR-0008 部署架构:爱思助手 + SideStore
- ✅ 部署链路完全就位(Runner.ipa 流程已验证)

## ⏱ 时间切片

### 🌅 上午(3h)
- 09:00-10:00 Stage 0 ROA + Stage 1 启动决策
- 10:00-11:00 Drift schema 设计 + 字段确认
- 11:00-12:00 pubspec.yaml 添加依赖 + flutter pub get

### ☀️ 下午(3h)
- 13:00-14:30 Drift schema 代码(`lib/data/db/app_database.dart`)
- 14:30-15:30 DAO 接口(TransactionDao / CategoryDao / AccountDao)
- 15:30-16:00 单元测试骨架

## 📝 关键命令

```bash
# 添加 Drift
flutter pub add drift sqlite3_flutter_libs path_provider path
flutter pub add --dev drift_dev build_runner

# 添加 Riverpod
flutter pub add flutter_riverpod riverpod_annotation
flutter pub add --dev riverpod_generator

# 添加实用依赖
flutter pub add intl vibration

# 代码生成(Drift + Riverpod)
dart run build_runner build --delete-conflicting-outputs

# 测试
flutter test
flutter analyze
```

## 📂 今日创建/修改文件

```
lib/
├── data/
│   ├── db/
│   │   ├── app_database.dart      (新建)
│   │   ├── tables/
│   │   │   ├── transactions.dart (新建)
│   │   │   ├── categories.dart   (新建)
│   │   │   └── accounts.dart     (新建)
│   │   └── daos/
│   │       ├── transaction_dao.dart
│   │       ├── category_dao.dart
│   │       └── account_dao.dart
│   └── repositories/
│       └── transaction_repository.dart (接口)

test/
└── data/
    └── db/
        └── app_database_test.dart (新建)
```

## 🐛 预计问题

| 问题 | 解决 |
|---|---|
| Drift 代码生成失败 | `dart run build_runner build --delete-conflicting-outputs` |
| sqlite3.dll 找不到(Windows) | `sqlite3_flutter_libs` 包自动处理 |
| build_runner 慢 | 后续增量 build 更快 |
| Drift schema 字段设计不准 | 先简单(version, created_at 必备),后续 Stage 扩展 |

## ✅ 验收清单

- [ ] pubspec.yaml 新增依赖已 commit
- [ ] Drift 3 表代码已生成
- [ ] DAO 接口定义完成
- [ ] `flutter analyze` 0 错误
- [ ] `flutter test` 通过(包括数据库 in-memory 测试)
- [ ] 准备 Day 5 Riverpod 骨架

---

**Day 4 启动清单**:把"5 秒 3 步手动记账"的目标写进 Drift schema。